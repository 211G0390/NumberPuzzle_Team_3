<Window x:Class="Puzzle_grafico.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:Puzzle_grafico.Models"
        xmlns:local="clr-namespace:Puzzle_grafico"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <models:ViewModel></models:ViewModel>
    </Window.DataContext>

    <StackPanel>
        <Canvas Name="canvas" Background="LightGray" Width="400" Height="400">
            <ItemsControl ItemsSource="{Binding TableroWPF}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding Columna}" />
                        <Setter Property="Canvas.Top" Value="{Binding Fila}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="100" Height="100" Background="Red">
                            <TextBlock Text="{Binding Valor}"  FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>

                        </Grid>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


        </Canvas>
        <Button Command="{Binding EmpezarCommand}">start</Button>

    </StackPanel>
</Window>
