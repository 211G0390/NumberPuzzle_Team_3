<Window x:Class="Puzzle_grafico.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:Puzzle_grafico.Models"
        xmlns:local="clr-namespace:Puzzle_grafico"
        mc:Ignorable="d"
        Background="#FF7289DA"
        Title="MainWindow" Height="500" Width="400">
    <Window.DataContext>
        <models:ViewModel></models:ViewModel>
    </Window.DataContext>


    <StackPanel  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">


        <TextBlock Text="9-Puzzle" FontSize="36" Foreground="White" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>




    <Canvas Name="canvas" Background="LightGray" Width="300" Height="300">
            <ItemsControl ItemsSource="{Binding TableroWPF}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding Columna}" />
                        <Setter Property="Canvas.Top" Value="{Binding Fila}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="100" Height="100" >                        
                            <Button >
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="FontSize" Value="28"/>
                                    <Setter Property="Width" Value="90"/>
                                    <Setter Property="Height" Value="90"/>
                                    <Setter Property="Background" Value="#FF7289DA"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#FF99AAB5"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                    <Setter Property="FontWeight" Value="Bold"/>

                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2"/>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            
                            <TextBlock Text="{Binding Valor}"  FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>

                        </Button>
                        </Grid>


                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


        </Canvas>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Mezclar" Command="{Binding MezclarCommand}" Margin="10" Background="#FF43B581" Width="100">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="FontSize" Value="20"/>
                        
                        
                        <Setter Property="Background" Value="#FF7289DA"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="#FF99AAB5"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="FontWeight" Value="Bold"/>

                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Resolver" Command="{Binding EmpezarCommand}" Margin="10" Background="#FFFAA61A" Width="100">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="FontSize" Value="20"/>


                            <Setter Property="Background" Value="#FF7289DA"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="#FF99AAB5"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="FontWeight" Value="Bold"/>

                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
            </Button>


        </StackPanel>

    </StackPanel>
</Window>
